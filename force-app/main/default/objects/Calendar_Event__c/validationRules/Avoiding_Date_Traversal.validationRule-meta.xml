<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Avoiding_Date_Traversal</fullName>
    <active>true</active>
    <description>Start and End times should belong to the same day (We are not allowing users to schedule events that take more than one day)</description>
    <errorConditionFormula>DATEVALUE(Start_Time__c) &lt;&gt; DATEVALUE(End_Time__c)</errorConditionFormula>
    <errorMessage>Your events can not span multiple dates</errorMessage>
</ValidationRule>
